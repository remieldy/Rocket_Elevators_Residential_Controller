

# class open_doors():
#     def __init__(self, open, closed):
#         self.open_doors = open_doors
#         self.open_doors = closed_doors

# class closed_doors():
#     def __init__(sefl, open, closed):
#         self.closed_doors = open_doors
#         self.closed_doors = closed_doors

#-----------------------------------------------------------------------------------------------------------------
# i define a doors Class with priority (itself, open and closed)
# i define my class doors with each attributes

    def open_doors(self, status, open, closed):
        self.open_doors = open_doors
        self.status = status
        self.open = open
        self.closed = closed


    def closed_doors(self, status, open, closed):
        self.closed_doors = closed_doors
        self.status = status
        self.open = open
        self.closed = closed
# -----------------------------------------------------------------------------------

    def __init__(self, status):
        self.doors = doors
        self.status = status
    def __init__(self, status, on):
        self.doors = doors
        self.status = status
        self.on = on
    def __init__(self, status, off):
        self.doors = doors
        self.status = status
        self.off = off



# i defie a class for the light inside elevator and i put priority ( itself, open, closed
# i difie my class with each attributes)

    def inside_light_elevator(self, status, open, closed):
        self.inside_light_elevator = inside_light_elevator
        self.status = status
        self.open = open
        self.closed = closed









